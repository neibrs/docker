version: "2"

syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  simplest-sync: #tip: add -sync and you keep consistent names as a convention
    src: './web'
    sync_host_ip: 'auto'
    sync_host_port: 10882
    sync_strategy: 'rsync'